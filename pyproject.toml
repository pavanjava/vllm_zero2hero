[project]
name = "vllm-zero2hero"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "vllm>=0.18.0",
    "torch>=2.10.0",
    "huggingface-hub>=1.7.2"
]
